export default function TicketsPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-neutral-900">Tickets</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Flight booking tracking arrives in build step 4. Real check-in/booking
        automation via the agency&apos;s GDS API is a later phase, not this one.
      </p>
    </div>
  );
}
