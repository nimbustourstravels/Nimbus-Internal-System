export default function TeamPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-neutral-900">Team</h1>
      <p className="mt-2 text-sm text-neutral-500">
        Per-employee workload rollup (admin-only) arrives in build step 8.
      </p>
    </div>
  );
}
